import React from "react";
import AppRoutes from "../index";

const component = () => shallow(<AppRoutes {...props} />);
describe("should render AppRoutes component", () => {
  it("should render", () => {});
});
