import React from "react";
import DesktopHeader from "../index";

const component = () => shallow(<DesktopHeader {...props} />);
describe("should render DesktopHeader component", () => {
  it("should render DesktopHeader", () => {});
});
